const form=document.getElementById('accessForm');const statusEl=document.getElementById('formStatus');const priority=form?.querySelector('input[value="priority"]');if(priority){priority.disabled=true;const copy=priority.closest('label')?.querySelector('small');if(copy)copy.textContent='Paid access returns after secure payment rotation';}
form?.addEventListener('submit',async(e)=>{e.preventDefault();const button=form.querySelector('button[type="submit"]');button.disabled=true;statusEl.textContent='Confirming access…';const data=Object.fromEntries(new FormData(form).entries());data.eventSlug='grown-ish-rose-on-piedmont';data.marketingConsent=Boolean(form.elements.marketingConsent.checked);data.ticketTypeId=data.ticketTypeId==='priority'?(window.KA_PRIORITY_TICKET_ID||'priority'):(window.KA_COMPLIMENTARY_TICKET_ID||'complimentary');data.requestKey=form.dataset.requestKey||(form.dataset.requestKey=crypto.randomUUID());try{const r=await fetch('/api/access/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const out=await r.json();if(!r.ok)throw new Error(out.error||'Unable to confirm access');if(out.checkoutUrl){window.location.href=out.checkoutUrl;return}window.location.href=out.redirect||'/success.html';}catch(err){statusEl.textContent=err.message;button.disabled=false;}});
