{
  "name": "111atl",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "111ATL standalone nightlife booking and lead capture site.",
  "scripts": {
    "build": "rm -rf public && mkdir -p public && cp index.html styles.css script.js public/",
    "start": "echo 'Serve with Vercel or any static server'"
  },
  "engines": {
    "node": "20.x"
  }
}
